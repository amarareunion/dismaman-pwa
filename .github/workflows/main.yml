name: 🚀 Deploy Dis Maman PWA

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout Repository
      uses: actions/checkout@v4
      
    - name: 📦 Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        
    - name: 🔧 Install Global Expo CLI
      run: npm install -g @expo/cli@latest
        
    - name: 📱 Install Dependencies
      run: |
        cd dismaman-complete/frontend
        npm install
        
    - name: 🔐 Create Environment File
      run: |
        cd dismaman-complete/frontend
        echo "EXPO_PUBLIC_BACKEND_URL=${{ secrets.EXPO_PUBLIC_BACKEND_URL }}" > .env
        echo "EXPO_PUBLIC_API_KEY=${{ secrets.EXPO_PUBLIC_API_KEY }}" >> .env
        echo "EXPO_PUBLIC_APP_ENV=production" >> .env
        
    - name: 🏗️ Build PWA
      run: |
        cd dismaman-complete/frontend
        npx expo export --platform web
        
    - name: 📄 Create CNAME file
      run: |
        cd dismaman-complete/frontend/dist
        echo "dismaman.com" > CNAME
        
    - name: 🚀 Deploy to GitHub Pages
      uses: peaceiris/actions-gh-pages@v3
      if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: ./dismaman-complete/frontend/dist
        
    - name: ✅ Deployment Summary
      run: |
        echo "🎉 PWA deployed successfully!"
        echo "📱 URL: https://dismaman.com"
        echo "🔧 Built with secrets from GitHub"
